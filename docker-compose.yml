services:
  api:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env
    environment:
      NODE_ENV: production
      DATABASE_URL: /app/data/arogya.db
    volumes:
      - arogya-data:/app/data
    restart: unless-stopped

volumes:
  arogya-data:
